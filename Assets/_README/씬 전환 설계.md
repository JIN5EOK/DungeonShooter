* SceneLoader 구상
    * '씬 이동 관리자'가 아님, 씬을 한번 로드할때 사용하는 일회성 객체
    * 함수
        * LoadScene() 씬을 로드한다
        * AddContext<T>(T context) 로드할 씬의 LifeTimeScope에 반영할 Context를 추가한다
    * 활용 예
        * AddContext<SceneData>(data); - 로드할 씬에 필요한 컨텍스트를 추가
        * LoadScene() - 최종적으로 씬을 로드한다